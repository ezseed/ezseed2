// Generated by CoffeeScript 1.6.2
(function() {
  var forEachDirectory;

  forEachDirectory = require("./forEachDirectory");

  module.exports = function(root, options, cb) {
    var paths, _ref;

    if (typeof options === 'function') {
      _ref = [{}, options], options = _ref[0], cb = _ref[1];
    }
    paths = [];
    return forEachDirectory(root, (function(root, name) {
      return paths.push(root + "/" + name);
    }), options, function(err) {
      if (err) {
        return cb(err);
      }
      return cb(null, paths);
    });
  };

}).call(this);

/*
//@ sourceMappingURL=getDirectories.map
*/
