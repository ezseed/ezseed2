// Generated by CoffeeScript 1.6.2
(function() {
  var forEachFile;

  forEachFile = require("./forEachFile");

  module.exports = function(root, options, cb) {
    var paths, _ref;

    if (typeof options === 'function') {
      _ref = [{}, options], options = _ref[0], cb = _ref[1];
    }
    paths = [];
    return forEachFile(root, (function(root, name) {
      return paths.push(root + "/" + name);
    }), options, function(err) {
      if (err) {
        return cb(err);
      }
      return cb(null, paths);
    });
  };

}).call(this);

/*
//@ sourceMappingURL=getFiles.map
*/
