<% include header %>

<h1>Torrents</h1>

<% if(user.client == 'transmission') { %>

	<section id="admin" style="padding:10px">
		<iframe src="http://<%= host %>:<%= user['rpc-port'] %>" width="100%" frameborder="0" height="500px" ></iframe>
	</section>

<% } else { %>

	<section id="admin" style="padding:10px">
		<iframe src="http://<%= host %>/rutorrent" width="100%" frameborder="0" height="500px" ></iframe>
	</section>

<% } %>

<% include footer %>